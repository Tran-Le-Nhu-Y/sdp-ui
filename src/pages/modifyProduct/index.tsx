//import { useTranslation } from 'react-i18next';
//import { CreateOrModifyForm } from '../../components';

export default function ModifyProductPage() {
	//const { t } = useTranslation();

	return (
		<div>
			{/* <CreateOrModifyForm title={''} label={`${t('productName')}`} /> */}
		</div>
	);
}
